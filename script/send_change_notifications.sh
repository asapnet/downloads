#!/bin/bash

script/rails runner "ContentNotifier.notify(1, 'Before I add the next batch of content (mostly on Core Data), I thought it would be wise to change the styling (based on some previous feedback) so the color coding and content are more obvious.  I would really appreciate comments or questions about the changes, especially in the section partitioning.')"